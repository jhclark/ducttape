# This is just a single command that writes output to
# stderr. Lines that begin with # are comments and
# comment blocks preceeding task declarations are associated
# with that task.
[ohai]
  echo hi
